# Project Rules

## Git Push Policy
- **Default push target is Forgejo only** (`git push origin main`).
- Do NOT push to GitHub (`github` remote) unless the user explicitly asks.
- The user controls when changes go public on GitHub after verifying they work.
